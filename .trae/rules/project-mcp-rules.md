---
alwaysApply: true
description: |
  - 1. Use `mui-mcp` to generate MUI v7+ components for all UI elements
    - . Use the MCP `mui-mcp` to improve the UI/UX, make greater use of MUI components, for example: 
      - INPUTS (Button, Button Group, Checkbox, Number Field, Select, Slider, Switch, Text Field, Textarea (if multi-line use outlined-multiline-static), etc.),
      - DATA DISPLAY (Avatar, Badge, Chip, Divider, Icons, List, etc.),
      - FEEDBACK (Alerts, Backdrops, Dialogs, Progress, Skeleton, Snackbar, etc.), 
      - NAVIGATION (Bottom Navigation, Breadcrumbs, Drawer, Link, Menu, Pagination, Speed Dial, Stepper, Tabs, etc.)
      - SURFACES (Accordion, AppBar, Card, Paper, etc.).
  - 2. Use `context7` MCP to search libraries and fetch documentation programmatically
  - 3. Use typescript to ensure type safety and improve code quality




